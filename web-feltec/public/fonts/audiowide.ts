import { Audiowide } from "next/font/google";

// Carrega a fonte Audiowide do Google Fonts
export const audiowide = Audiowide({
  subsets: ["latin"],
  weight: "400", // Audiowide só tem um peso mesmo
  display: "swap",
});